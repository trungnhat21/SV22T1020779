@* @model ChangeRole
@{
    ViewBag.Title = "Phân quyền nhân viên";
}

@section PageToolBar {
    @if (Model.IsWorking == false)
    {
        <button type="submit" form="formChangeRole" class="btn btn-primary">
            <i class="bi bi-shield-check me-1"></i> Lưu phân quyền
        </button>
    }
    <a href="~/Employee" class="btn btn-secondary ms-1">
        <i class="bi bi-arrow-left"></i> Quay lại
    </a>
}

<div class="row justify-content-center">
    <div class="col-12">
        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger pb-0">
                <ul class="list-unstyled">
                    @foreach (var modelState in ViewData.ModelState.Values)
                    {
                        foreach (var error in modelState.Errors)
                        {
                            <li class="mb-2"><i class="bi bi-dot"></i> @error.ErrorMessage</li>
                        }
                    }
                </ul>
            </div>
        }

        <div class="card card-primary card-outline">
            <div class="card-body">
                <form id="formChangeRole" method="post" asp-action="ChangeRole">
                    <input type="hidden" asp-for="EmployeeID" />
                    <input type="hidden" asp-for="IsWorking" />

                    <fieldset class="mb-4">
                        <legend class="fs-6 text-muted mb-3"><i class="bi bi-person me-1"></i> Thông tin nhân viên</legend>
                        <dl class="row mb-0">
                            <dt class="col-sm-3">Họ và tên</dt>
                            <dd class="col-sm-9">@Model.FullName</dd>

                            <dt class="col-sm-3">Email</dt>
                            <dd class="col-sm-9">@Model.Email</dd>

                            <dt class="col-sm-3">Trạng thái</dt>
                            <dd class="col-sm-9">
                                @if (Model.IsWorking == false)
                                {
                                    <span class="badge bg-success">Đang làm việc</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">Đã nghỉ việc</span>
                                }
                            </dd>
                        </dl>
                    </fieldset>

                    @if (Model.IsWorking == false)
                    {
                        <fieldset>
                            <legend class="fs-6 text-muted mb-3"><i class="bi bi-shield-lock me-1"></i> Danh sách quyền</legend>
                            <div class="table-responsive">
                                <table class="table table-bordered table-hover align-middle" id="table-roles">
                                    <thead class="table-secondary">
                                        <tr>
                                            <th style="width:60px;" class="text-center">Chọn</th>
                                            <th>Tên quyền</th>
                                            <th>Mô tả quyền</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @for (int i = 0; i < Model.Roles.Count; i++)
                                        {
                                            <tr>
                                                <td class="text-center">
                                                    <input type="hidden" asp-for="@Model.Roles[i].RoleName" />

                                                    @if (Model.IsWorking == false)
                                                    {
                                                        <input type="checkbox"
                                                               class="form-check-input role-checkbox"
                                                               asp-for="@Model.Roles[i].IsSelected" />
                                                    }
                                                    else
                                                    {
                                                        @if (Model.Roles[i].IsSelected)
                                                        {
                                                            <input type="checkbox" class="form-check-input" checked disabled />
                                                        }
                                                        else
                                                        {
                                                            <input type="checkbox" class="form-check-input" disabled />
                                                        }
                                                        <input type="hidden" asp-for="@Model.Roles[i].IsSelected" />
                                                    }
                                                </td>
                                                <td>@Model.Roles[i].RoleName</td>
                                                <td class="text-muted">@Model.Roles[i].Description</td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </fieldset>
                    }
                </form>
            </div>
        </div>
    </div>
</div> *@
@model Employee
@{
    ViewBag.Title = "Phân quyền nhân viên";

    // 3 roles theo WebUserRoles
    var allRoles = new[]
    {
        new { Name = WebUserRoles.Administrator, Desc = "Quản trị — toàn quyền hệ thống" },
        new { Name = WebUserRoles.DataManager,   Desc = "Quản lý dữ liệu — CRUD danh mục, hàng hóa, đối tác" },
        new { Name = WebUserRoles.Sales,         Desc = "Quản lý bán hàng — xử lý đơn hàng" },
    };

    // Lấy danh sách quyền hiện tại của nhân viên từ DB (thông qua Employee.RoleNames nếu có)
    // Tuy nhiên Employee model không có RoleNames, nên dùng ViewBag nếu cần.
    // Hiện tại chỉ render checkbox, không pre-check (có thể mở rộng sau).
}

@section PageToolBar {
    <button type="submit" form="formChangeRole" class="btn btn-primary">
        <i class="bi bi-shield-check me-1"></i>
        Lưu phân quyền
    </button>
    <a href="~/Employee" class="btn btn-secondary ms-1">
        <i class="bi bi-arrow-left"></i>
        Quay lại
    </a>
}

<div class="row justify-content-center">
    <div class="col-12">
        <div class="card card-primary card-outline">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="bi bi-shield-check me-2"></i>
                    Phân quyền cho nhân viên
                </h5>
            </div>

            <div class="card-body">
                <form id="formChangeRole" method="post"
                      action="~/Employee/ChangeRole/@Model.EmployeeID">

                    <fieldset class="mb-4">
                        <legend class="fs-6 text-muted mb-3">
                            <i class="bi bi-person me-1"></i>
                            Thông tin nhân viên
                        </legend>
                        <dl class="row mb-0">
                            <dt class="col-sm-3">Họ và tên</dt>
                            <dd class="col-sm-9">@Model.FullName</dd>
                            <dt class="col-sm-3">Email</dt>
                            <dd class="col-sm-9">@Model.Email</dd>
                            <dt class="col-sm-3">Trạng thái</dt>
                            <dd class="col-sm-9">
                                @if (!Model.IsWorking)
                                {
                                    <span class="badge bg-success">Đang làm việc</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">Đã nghỉ việc</span>
                                }
                            </dd>
                        </dl>
                    </fieldset>

                    <fieldset>
                        <legend class="fs-6 text-muted mb-3">
                            <i class="bi bi-shield-lock me-1"></i>
                            Danh sách quyền
                        </legend>
                        <div class="table-responsive">
                            <table class="table table-bordered table-hover align-middle">
                                <thead class="table-secondary">
                                    <tr>
                                        <th style="width:60px;" class="text-center">Chọn</th>
                                        <th>Tên quyền</th>
                                        <th>Mô tả quyền</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var role in allRoles)
                                    {
                                        // Lấy danh sách quyền hiện tại từ ViewBag (controller truyền vào)
                                        var currentRoles = ViewBag.CurrentRoles as List<string> ?? new List<string>();
                                        bool isChecked = currentRoles.Contains(role.Name);
                                        <tr>
                                            <td class="text-center">
                                                <input class="form-check-input"
                                                       type="checkbox"
                                                       name="roleNames"
                                                       value="@role.Name"
                                                       @(isChecked ? "checked" : "") />
                                            </td>
                                            <td>@role.Name</td>
                                            <td>@role.Desc</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    </fieldset>

                </form>
            </div>
        </div>
    </div>
</div>